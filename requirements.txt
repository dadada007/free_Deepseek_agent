# Hermes Agent - Python 依赖
# 安装: pip install -r requirements.txt

# HTTP 客户端 - 连接 DeepSeek Browser API
requests>=2.28.0

# PyQt6 - 可选 GUI 界面 (如需使用 --qt 参数)
# 如果使用默认 tkinter 界面，可以注释掉下面这行
PyQt6>=6.4.0

# 标准库依赖 (Python 内置，无需额外安装):
# - argparse, ast, collections, concurrent.futures, dataclasses
# - datetime, enum, hashlib, json, logging, operator, os
# - pathlib, queue, re, sqlite3, subprocess, sys, tempfile
# - threading, time, tkinter, typing, urllib.request
