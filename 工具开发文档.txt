[{"name": "write_file", "arguments": {"file_path": "D:/9/hermes/如何增加新工具.md", "content": "# Hermes 工具开发指南\n\n本文档说明如何在 Hermes 中增加新工具，实现热加载、即写即测。\n\n---\n\n## 📁 目录结构\n\n```\nhermes/\n├── agent_tools/              # 所有工具实现放在这里\n│   ├── __init__.py\n│   ├── hot_reload.py         # 热加载器（自动监听文件变化）\n│   ├── fs_register.py        # 工具注册核心\n│   ├── file_operations_tools.py\n│   ├── health_tools.py       # 示例：健康检查工具\n│   └── dir_reader_tools.py   # 示例：目录阅读工具\n├── tools.py                  # 工具注册表（TOOLS 字典）\n├── tool_index.py             # 工具索引（分类管理）\n└── run.py                    # 启动入口\n```\n\n---\n\n## 🚀 快速开始：创建一个新工具\n\n### 第 1 步：在 `agent_tools/` 下创建新文件\n\n文件名规范：`{功能描述}_tools.py`，例如 `my_tools.py`\n\n```python\n# -*- coding: utf-8 -*-\n\"\"\"\n工具描述：这里写清楚这个工具是做什么的\n\"\"\"\n\nimport os\nimport sys\n\n# 导入全局注册函数\nsys.path.insert(0, os.path.dirname(os.path.dirname(os.path.abspath(__file__))))\nfrom tools import register\n\n\n# ==================== 工具实现函数 ====================\n\ndef my_function(args: dict) -> str:\n    \"\"\"\n    工具的核心逻辑。\n    参数: args 是字典，包含用户传入的参数\n    返回: 字符串（工具执行结果）\n    \"\"\"\n    # 1. 获取参数\n    param = args.get('param_name', 'default_value')\n    \n    # 2. 执行业务逻辑\n    result = f\"处理结果: {param}\"\n    \n    # 3. 返回字符串结果\n    return result\n\n\n# ==================== 工具注册 ====================\n\ndef register_tools() -> int:\n    \"\"\"\n    注册此模块中的所有工具。\n    热加载器会自动调用此函数。\n    返回注册的工具数量。\n    \"\"\"\n    register(\n        name=\"my_tool\",                    # 工具名称（唯一标识）\n        description=\"工具描述：说明功能和使用场景\",  # 描述（LLM 会根据这个决定是否调用）\n        parameters={                       # 参数定义（JSON Schema 格式）\n            \"type\": \"object\",\n            \"properties\": {\n                \"param_name\": {\n                    \"type\": \"string\",\n                    \"description\": \"参数的说明\",\n                    \"default\": \"default_value\"\n                }\n            },\n            \"required\": [\"param_name\"]     # 必填参数列表\n        },\n        func=my_function                   # 工具实现函数\n    )\n    return 1  # 返回注册的工具数量\n```\n\n### 第 2 步：等待热加载\n\n- 热加载器每 **3 秒** 扫描一次 `agent_tools/` 目录\n- 文件保存后自动检测变化，自动调用 `register_tools()`\n- **无需重启 Hermes**\n\n### 第 3 步：测试工具\n\n在对话中直接调用：\n\n```\n用户：调用 my_tool，参数 param_name=\"test\"\n```\n\n或在代码中测试：\n\n```python\n# 直接导入测试\nfrom tools import execute\nresult = execute(\"my_tool\", {\"param_name\": \"test\"})\nprint(result)\n```\n\n---\n\n## 📐 工具设计规范\n\n### 1. 命名规范\n\n| 类型 | 规范 | 示例 |\n|------|------|------|\n| 文件名 | `{功能}_tools.py` | `health_tools.py` |\n| 工具名 | 小写 + 下划线 | `health_check`, `read_directory` |\n| 函数名 | 小写 + 下划线 | `_read_directory`, `_generate_report` |\n| 注册函数 | 固定为 `register_tools()` | - |\n\n### 2. 参数定义规范\n\n使用 JSON Schema 格式：\n\n```python\nparameters={\n    \"type\": \"object\",\n    \"properties\": {\n        \"path\": {\n            \"type\": \"string\",\n            \"description\": \"目录路径，使用正斜杠或双反斜杠\"\n        },\n        \"depth\": {\n            \"type\": \"integer\",\n            \"description\": \"扫描深度，默认 3\",\n            \"default\": 3,\n            \"minimum\": 1,\n            \"maximum\": 6\n        },\n        \"verbose\": {\n            \"type\": \"boolean\",\n            \"description\": \"是否显示详细信息\",\n            \"default\": False\n        }\n    },\n    \"required\": [\"path\"]\n}\n```\n\n### 3. 返回值规范\n\n- **必须返回字符串**（`str` 类型）\n- 格式清晰，包含必要的换行和缩进\n- 错误信息以 `❌` 开头\n- 成功信息包含必要的统计数据\n\n```python\n# ✅ 好的返回\ndef my_tool(args):\n    if error:\n        return f\"❌ 错误: {error_msg}\"\n    return f\"✅ 成功: 处理了 {count} 个文件\\n详情: {details}\"\n\n# ❌ 不好的返回\ndef my_tool(args):\n    return {\"status\": \"ok\"}  # 不能返回字典\n    return None              # 不能返回 None\n```\n\n### 4. 错误处理规范\n\n```python\ndef my_tool(args):\n    # 参数校验\n    path = args.get('path', '').strip()\n    if not path:\n        return \"❌ 错误: 请提供 path 参数\"\n    \n    # 路径校验\n    if not os.path.exists(path):\n        return f\"❌ 路径不存在: {path}\"\n    \n    try:\n        # 业务逻辑\n        result = do_something(path)\n        return f\"✅ 成功: {result}\"\n    except PermissionError:\n        return f\"❌ 权限不足: 无法访问 {path}\"\n    except Exception as e:\n        return f\"❌ 执行失败: {str(e)}\"\n```\n\n---\n\n## 🔧 工具分类注册\n\n在 `tool_index.py` 的 `CATEGORIES` 字典中添加分类：\n\n```python\nCATEGORIES = {\n    # ... 现有分类 ...\n    \"你的分类名\": [\n        \"my_tool\",\n        \"another_tool\",\n    ],\n}\n```\n\n---\n\n## 🧪 测试方法\n\n### 方法 1：在文件末尾添加测试代码\n\n```python\nif __name__ == \"__main__\":\n    register_tools()\n    print(\"✅ my_tools 已加载\")\n    # 测试工具\n    result = my_function({\"param_name\": \"test\"})\n    print(result)\n```\n\n运行测试：\n\n```bash\npython -m agent_tools.my_tools\n```\n\n### 方法 2：在对话中测试\n\n启动 Hermes 后，在聊天中输入：\n\n```\n帮我用 my_tool 处理一下，参数 param_name=\"test\"\n```\n\n### 方法 3：使用 Python 交互式测试\n\n```python\n>>> from tools import execute\n>>> result = execute(\"my_tool\", {\"param_name\": \"test\"})\n>>> print(result)\n```\n\n---\n\n## 📦 工具模板速查\n\n### 基础模板（无参数）\n\n```python\n# -*- coding: utf-8 -*-\n\"\"\"简单工具示例\"\"\"\nimport sys, os\nsys.path.insert(0, os.path.dirname(os.path.dirname(os.path.abspath(__file__))))\nfrom tools import register\n\ndef hello(args: dict) -> str:\n    return \"Hello, World!\"\n\ndef register_tools() -> int:\n    register(\n        name=\"hello\",\n        description=\"返回 Hello World 问候\",\n        parameters={\"type\": \"object\", \"properties\": {}, \"required\": []},\n        func=hello\n    )\n    return 1\n```\n\n### 文件操作模板\n\n```python\n# -*- coding: utf-8 -*-\n\"\"\"文件操作工具示例\"\"\"\nimport os\nimport sys\nsys.path.insert(0, os.path.dirname(os.path.dirname(os.path.abspath(__file__))))\nfrom tools import register\n\ndef read_file_content(args: dict) -> str:\n    path = args.get('path', '').strip()\n    if not path:\n        return \"❌ 请提供 path 参数\"\n    if not os.path.exists(path):\n        return f\"❌ 文件不存在: {path}\"\n    try:\n        with open(path, 'r', encoding='utf-8') as f:\n            content = f.read()\n        return f\"✅ 读取成功\\n\\n{content[:500]}\"\n    except Exception as e:\n        return f\"❌ 读取失败: {e}\"\n\ndef register_tools() -> int:\n    register(\n        name=\"read_small_file\",\n        description=\"读取小文件内容（最多500字符）\",\n        parameters={\n            \"type\": \"object\",\n            \"properties\": {\n                \"path\": {\"type\": \"string\", \"description\": \"文件路径\"}\n            },\n            \"required\": [\"path\"]\n        },\n        func=read_file_content\n    )\n    return 1\n```\n\n---\n\n## ⚠️ 常见问题\n\n### Q1: 工具没有出现\n\n- 检查文件名是否以 `_tools.py` 结尾\n- 检查是否有 `register_tools()` 函数\n- 查看日志：热加载器会打印加载状态\n- 确认文件在 `agent_tools/` 目录下\n\n### Q2: 修改后工具不更新\n\n- 等待 3 秒（热加载间隔）\n- 检查 Python 语法是否正确\n- 查看终端日志是否有错误信息\n- 可以手动触发：调用 `reload_all()` 函数\n\n### Q3: 参数解析错误\n\n- 确保参数名与注册时一致\n- 检查 JSON Schema 格式是否正确\n- 在 `tools.py` 的 `_repair_tool_args` 中查看参数修复逻辑\n\n### Q4: Windows 路径问题\n\n- 使用正斜杠：`C:/path/to/file`\n- 或使用双反斜杠：`C:\\\\path\\\\to\\\\file`\n- 禁止使用单反斜杠：`C:\\path\\to\\file` ❌\n\n---\n\n## 📝 工具清单（已实现）\n\n| 工具名 | 文件 | 功能 |\n|--------|------|------|\n| `health_check` | `health_tools.py` | 项目健康检查 |\n| `read_directory` | `dir_reader_tools.py` | 树形目录阅读 |\n\n---\n\n## 🔗 相关文件\n\n- `tools.py` — 工具注册表（`TOOLS` 字典）\n- `tool_index.py` — 工具分类索引\n- `agent_tools/hot_reload.py` — 热加载器\n- `agent_tools/fs_register.py` — 注册辅助函数\n\n---\n\n*文档版本: 1.0 | 更新日期: 2026-08-17*\n"}]}